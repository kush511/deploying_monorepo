{
  "name": "turbo-deploy-try",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:generate":"cd packages/db && bunx prisma generate && cd ../..",
    "start:web":"cd apps/web && run bun start",
    "start:backend":"cd apps/backend && run bun start",
    "start:ws":"cd apps/ws && run bun start"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "prisma": "6.16.0",
    "turbo": "^2.7.6",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.3.6",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@prisma/client": "6.16.0",
    "@types/express": "^5.0.6",
    "express": "^5.2.1"
  }
}
